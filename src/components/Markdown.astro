---
import { Markdown } from 'astro-remote'

const response = await fetch(
  'https://raw.githubusercontent.com/madrus/mdocs/master/README.md'
)
const markdown = await response.text()
---

<Markdown>
  {markdown}
</Markdown>

<style>
  img {
    max-width: 100%;
  }
</style>
